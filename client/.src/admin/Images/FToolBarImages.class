' Gambas class file

Private form_config_toolbar As FConfigToolbar

Public Sub Init()
  
  Dim bShowIconpath As Boolean = True  
  
  With form_config_toolbar = New FConfigToolbar(Vbox_ConfigToolbar)
      .Init("Admin")
      .Expand = True
  End With
  
  
End

