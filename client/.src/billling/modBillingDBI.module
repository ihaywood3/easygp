' Gambas module file


Public Function Descriptors_Get(Optional descriptor As String = "", limit As Integer = 100) As Collection

   Dim sql As String
   sql = "Select * from clerical.schedule where descriptor ILIKE $$%" & descriptor
   sql &= "%$$ order by cast(mbs_item as int), descriptor LIMIT " & limit
  Return modDBConnect.exec_query_collection(sql)
   
End
