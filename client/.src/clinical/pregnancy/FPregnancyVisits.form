# Gambas Form File 3.0

{ Form Form
  MoveScaled(0,0,128,92)
  Tag = "visits"
  { VBox1 VBox
    MoveScaled(4,4,118,84)
    Background = Color.TextBackground
    Expand = True
    { TableView1 TableView
      MoveScaled(11,10,87,54)
      Expand = True
      Mode = Select.Single
      Header = GridView.Horizontal
    }
  }
}
