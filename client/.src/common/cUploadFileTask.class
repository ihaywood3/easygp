' Gambas class file

Inherits CShellTask

Private doc As Collection

Public Sub _new(doc2 As Collection)

  doc = doc2
   
End

Public Sub Run_Final()
   Log.DebugMsg(Subst$("task &1 called the real Run_Final for cShellTask", Str$(Me)))
  modArchiveDocumentsDBI.Save(doc)
  modDBConnect.CommitTrans() 
   
End
