cache documents so that when switching type, load from cache
put in a refresh button
put in key validation eg integer for minutes processing in edit area
there is some sort of error in the referencing to lu_categories.pk
probably should allow branches to be the provider of HL7, not the head organisation, 
eg GP Access has an after hours.

April 10

when patient unmatched - lookup if previously noticed, if matched, use the proper fk-Patient